# Navigate to the project folder
cd game-generator

# Initialize git
git init

# Add all files
git add .

# Commit
git commit -m "Initial commit: Game Generator app"

# Add remote (replace USERNAME with your GitHub username)
git remote add origin https://github.com/USERNAME/game-generator.git

# Push to GitHub
git branch -M main
git push -u origin main
